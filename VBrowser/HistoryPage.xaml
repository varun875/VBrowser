<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VBrowser.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Background>
        <SolidColorBrush Color="{ThemeResource SystemAltHighColor}"/>
    </Page.Background>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Width="40" Height="40" CornerRadius="20" Click="BackButton_Click">
                    <SymbolIcon Symbol="Back"/>
                </Button>
                <TextBlock Text="History" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <Button
                Grid.Column="1"
                Content="Clear History"
                Click="ClearHistory_Click"
                VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <TextBlock
                x:Name="EmptyStateText"
                Text="No browsing history yet."
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Opacity="0.72"/>

            <ListView
                x:Name="HistoryList"
                ItemsSource="{x:Bind HistoryItems, Mode=OneWay}"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:HistoryEntry">
                        <Border
                            Margin="0,0,0,8"
                            Padding="12"
                            CornerRadius="8"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Spacing="4">
                                    <TextBlock
                                        Text="{x:Bind Title}"
                                        FontWeight="SemiBold"
                                        TextTrimming="CharacterEllipsis"/>
                                    <TextBlock
                                        Text="{x:Bind Url}"
                                        FontSize="12"
                                        Opacity="0.72"
                                        TextTrimming="CharacterEllipsis"/>
                                    <TextBlock
                                        Text="{x:Bind VisitedAt}"
                                        FontSize="12"
                                        Opacity="0.6"/>
                                </StackPanel>

                                <Button
                                    Grid.Column="1"
                                    Content="Open"
                                    Click="OpenHistoryItem_Click"
                                    Margin="12,0,0,0"
                                    VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>